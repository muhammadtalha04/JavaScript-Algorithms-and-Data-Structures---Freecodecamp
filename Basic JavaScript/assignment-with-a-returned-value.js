/*
  In this example I learnt about storing the returned value of a function in a variable.
*/

// Setup
var processed = 0;

function processArg(num) {
  return (num + 3) / 5;
}

// Only change code below this line

processed = processArg(7);