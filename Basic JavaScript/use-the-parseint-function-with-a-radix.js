/*
  This example was about converting a string to an integer by using radix as an argument in parseInt function.
*/

function convertToInteger(str) {
  return parseInt(str, 2);
}

convertToInteger("10011");