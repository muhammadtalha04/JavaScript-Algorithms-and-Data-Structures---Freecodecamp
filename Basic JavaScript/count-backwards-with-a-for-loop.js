/*
  This example was about iterating the for loop from 9 to 1 to store the odd numbers in an array.
*/

// Setup
var myArray = [];

// Only change code below this line
for (var i=9; i>=1; i-=2) {
  myArray.push(i);
}